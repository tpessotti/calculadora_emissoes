"""
Core calculation framework — pure Python, framework-agnostic.

No Django, no Streamlit, no UI concerns.  All public functions
are pure and raise typed exceptions rather than calling UI widgets.
"""
